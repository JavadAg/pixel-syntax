import { Minus, Square, X } from "lucide-react"

const WindowsControl = () => {
  return (
    <div className="ml-auto mr-0 flex items-center gap-3">
      <Minus size={13} />
      <Square size={13} />
      <X size={18} />
    </div>
  )
}

export default WindowsControl
